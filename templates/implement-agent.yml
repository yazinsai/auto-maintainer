name: Implement Agent

on:
  issues:
    types: [labeled]

permissions:
  contents: write
  issues: write
  pull-requests: write
  actions: read

concurrency:
  group: "implement-${{ github.event.issue.number }}"

jobs:
  generate-token:
    if: >-
      github.event.label.name == 'state:planned'
      && !contains(toJSON(github.event.issue.labels.*.name), 'risk:high')
    runs-on: ubuntu-latest
    outputs:
      token: "${{ steps.app-token.outputs.token }}"
    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: "${{ secrets.RPB_APP_ID }}"
          private-key: "${{ secrets.RPB_APP_PRIVATE_KEY }}"

  implement:
    needs: generate-token
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Read repo policy
        id: policy
        run: |
          if [ -f .github/repo-policy.md ]; then
            {
              echo 'content<<POLICY_EOF'
              cat .github/repo-policy.md
              echo 'POLICY_EOF'
            } >> "$GITHUB_OUTPUT"
          else
            echo "content=" >> "$GITHUB_OUTPUT"
          fi

      - name: Run implement agent
        uses: "anthropics/claude-code-action@{{CLAUDE_ACTION_SHA}}"
        with:
          prompt: |
            {{SYSTEM_PROMPT_IMPLEMENT}}

            --- REPO POLICY ---
            ${{ steps.policy.outputs.content }}
          claude_args: "--allowedTools Edit Read Write Grep Glob Bash(git:*) --max-turns 40 --model claude-sonnet-4-6"
          github_token: "${{ needs.generate-token.outputs.token }}"
        env:
          ANTHROPIC_API_KEY: "${{ secrets.ANTHROPIC_API_KEY }}"
          CLAUDE_CODE_OAUTH_TOKEN: "${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}"

      - name: Create PR if commits were pushed
        env:
          GH_TOKEN: "${{ needs.generate-token.outputs.token }}"
        run: |
          # Check if there are new commits beyond the initial checkout
          if [ "$(git rev-list --count HEAD ^origin/${{ github.event.repository.default_branch }})" -gt 0 ]; then
            BRANCH="implement/issue-${{ github.event.issue.number }}"
            git checkout -b "$BRANCH"
            git push origin "$BRANCH"

            # Collect labels from the issue
            LABELS=$(gh issue view "${{ github.event.issue.number }}" --json labels --jq '[.labels[].name] | join(",")')

            gh pr create \
              --title "fix: resolve #${{ github.event.issue.number }}" \
              --body "Closes #${{ github.event.issue.number }}" \
              --label "$LABELS" \
              --head "$BRANCH"
          fi
